import { describe, expect, it } from 'vitest';
import ExtensionOverlays from '../extension-overlays.svelte';

describe('ExtensionOverlays component', () => {
  it('exports component definition', () => {
    expect(ExtensionOverlays).toBeDefined();
  });
});
